# First Command Code / DeepSeek Prompt

You are implementing **Little Blue World — for Alia**, a specification-driven native Android project.

Before doing anything:

1. Read `AGENTS.md`.
2. Read:
   - `docs/PRODUCT.md`
   - `docs/ARCHITECTURE.md`
   - `docs/ROADMAP.md`
   - `docs/ACCEPTANCE_CRITERIA.md`
3. Inspect the current repository and report what already exists.
4. Do **not** edit any file yet.

Your task for this turn is **planning only for Milestone 0 — Repository Bootstrap**.

Produce a concise implementation plan covering:

- current repository state;
- package/application setup you propose;
- stable Android/Kotlin/AGP/Gradle choices you intend to use;
- `minSdk = 23`;
- Views/XML + ViewBinding setup;
- single-Activity skeleton;
- initial dependency list and why each dependency is needed;
- debug/release build setup;
- folder/package structure created now versus deferred;
- Gradle commands you will use to verify the milestone;
- risks or assumptions;
- any specification conflict you found.

Hard rules:

- Kotlin only.
- No Jetpack Compose.
- No Fragment/NavComponent.
- No game engine.
- No dependency not justified by the docs.
- Do not implement later milestones.
- Do not create personal copy or visual behavior yet.
- Do not claim anything works without running the appropriate build after implementation.

Stop after the plan and wait for approval before editing.
